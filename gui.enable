sudo make-auto-login-console -r # 
# sudo systemctl start gdm.service # && sudo make-auto-login-console -r # 
# sudo systemctl start getty@tty1.service
# sudo systemctl set-default multi-user.target # Grafik aus.
sudo systemctl set-default graphical.target # Grafik wieder ein.
sudo reboot
# gnome-session-quit --logout --force
# startx
